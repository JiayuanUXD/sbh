# Task Packet：OPT-026 房源列表查询与预取优化

> 状态：进行中  
> 创建日期：2026-08-07  
> 最后更新：2026-08-07

## 1. 目标

缓存规范化房源搜索，使用轻量区域选项查询替代完整首页聚合，并阻止高基数筛选链接自动预取。

## 2. 非目标

- 不改数据库级有效房源分页、计数和 1000 条候选上限。
- 不改筛选、排序、分页和统一有效供给业务口径。
- 不改 Collection、字段、索引、约束或迁移。
- 不提交、推送、创建 PR 或部署。

## 3. 权威上下文

- Task：`specs/frontend-mvp/task-details/F3-home-list.md`
- 页面 PRD：`docs/prd/前台网站_MVP_页面PRD/02-房源列表_PRD.md`
- Design：`specs/frontend-mvp/design.md` §9、§14
- Agent：`core.md` + `frontend.md` + `supply.md` + `testing.md`

## 4. 当前行为与证据

- 复现路径：生产模式访问 `/listings`、筛选 URL、分页 URL；首页滚动后查看 RSC 请求。
- 当前结果：列表 TTFB 约 1.18–1.27 秒，分页导航约 1.26 秒；列表额外调用完整 `getHomepage`；首页滚动可触发 25 个 RSC 请求。
- 期望结果：规范化重复查询命中标签缓存；区域选项仅调用有效行政区查询；筛选入口不自动预取。
- 修改前证据：本任务前置性能走查记录及设计文档 §1。

## 5. 影响范围

- 预计修改文件：Public Catalog facade/index、缓存封装、列表路由、首页/导航/筛选链接、测试和证据。
- 数据模型/迁移：无。
- 权限：无变化。
- API/路由：URL 和响应契约不变。
- 缓存/事件：listing 搜索与区域选项最长 300 秒；复用既有类别 tag 失效。
- 风险：首次冷搜索仍受候选扫描限制；关闭预取后首次点击不再提前抢跑。

## 6. 实施清单

- [ ] 建立失败测试或可复现用例。
- [ ] 实现轻量区域选项 facade 与缓存。
- [ ] 列表接入规范化查询缓存。
- [ ] 关闭高成本筛选链接自动预取。
- [ ] 验证筛选、分页、空状态、键盘和控制台。
- [ ] 更新文档与任务状态。

## 7. 验收

- 对应 PRD 条款：FP-02 §3、§5、§7、§8。
- 自动化测试：focused Vitest、统一有效供给回归、TypeScript、完整测试和构建。
- 浏览器路径：`/`、`/listings`、一种筛选、第二页、空结果；375/768/1440/1920 四档视口。
- 数据/迁移检查：无数据写入、无迁移。

## 8. 结果

- 修改文件：待完成。
- 实际结果：待完成。
- 验证摘要：待完成。
- 详细证据：`../../artifacts/verification/OPT-026/README.md`
- 剩余风险：数据库级分页属于后续任务。
- 下一步：OPT-027 首页与 sitemap 缓存失效。

# Task Packet：OPT-005～OPT-008 数据正确性修复

> 状态：部分完成
> 日期：2026-07-26

## 已完成

- OPT-005：公开目录和楼盘聚合完整分页读取候选，精筛后再执行全局排序、分页与聚合。
- OPT-006：举报暂停记录完整分页；任一页查询失败时向上抛错，禁止使用部分排除集。
- OPT-007：按同一 `asOf` 查询唯一半开区间关系，并从命中关系读取商户资格；0 条或多条均 fail-closed。
- OPT-008：房源、楼盘和内容页均完整分页枚举，不再静默截断。

## 剩余

- 超过 50,000 URL 时的 sitemap index/分片仍需独立路由改造。本轮已解决 URL 数据源截断，但不将 OPT-008 标记为全部完成。

## 验证

- `pnpm test`：93 个测试文件、1873 个测试通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- 浏览器：`/listings` 显示 8 套；房源详情正常；`/sitemap.xml` HTTP 200，包含房源、楼盘和内容页 URL。

/**
 * 领域：追加式审计（domain/audit）
 *
 * 职责边界（AGENTS.md §4, §5.5, §10, tasks.md M8.1-M8.2）：
 *   - 审计日志只允许追加和读取，不提供 update / delete
 *   - 保存主体 / 角色 / 组织快照、对象版本、前后值、请求上下文和结果
 *   - 禁止 Admin UI 和 API 修改或删除
 *   - 对日志详情、敏感值查看和导出本身再次审计
 *   - 高风险操作审计失败时业务操作必须失败
 *
 * 覆盖范围：
 *   - 审核、发布、下架、商户冻结、举报、分配、认领、转派、权限和账号停用
 *
 * M0 阶段：仅占位。
 */
export const DOMAIN_TAG = 'audit' as const

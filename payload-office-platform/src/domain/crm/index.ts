/**
 * 领域：客户、线索、归属和跟进（domain/crm）
 *
 * 职责边界（AGENTS.md §4, §5.3, §5.4, §5.5, §5.6, tasks.md M5）：
 *   - Customers：按手机号去重 / 30 天重复线索窗口
 *   - Leads：阶段（新建 / 待分配 / 跟进中 / 有效商机 / 带看 / 谈判 / 已转化 / 已流失）
 *   - 归属状态（未分配 / 已分配 / 公海）独立于阶段
 *   - 归属历史：不可改写，分配 / 认领 / 转派 / 回收均追加
 *   - FollowUps：不可删除；24 小时内纠错通过追加修正记录
 *   - MVP-R1 参数快照（runtime_policy_version、分配时限、活跃上限等）
 *
 * 业务不变量：
 *   - 公海不是线索阶段
 *   - 合并状态不是线索生命周期
 *   - 跟进“已推荐”必须关联统一有效供给中的至少一套房源
 *
 * M0 阶段：仅占位。
 */
export const DOMAIN_TAG = 'crm' as const

/**
 * 领域：房源举报（domain/report）
 *
 * 职责边界（AGENTS.md §4, §5.2, tasks.md M6.1-M6.2）：
 *   - 举报原因、证据、处理状态、处理阶段版本、负责人、结论
 *   - 状态机：分诊 → 领取 → 核实 → 等待资料 → 提交复核 → 关闭
 *   - 有效举报暂停只影响统一有效供给谓词
 *   - 不改写审核状态和发布状态（业务不变量）
 *   - 恢复和关闭要求权限、原因和审计
 *
 * M0 阶段：仅占位。
 */
export const DOMAIN_TAG = 'report' as const

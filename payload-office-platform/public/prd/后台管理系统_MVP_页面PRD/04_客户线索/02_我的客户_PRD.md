# 02 我的客户 PRD

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 编号 | 04-02 |
| 版本 | V1.1 |
| 状态 | 评审修订 |
| 所属模块 | 客户线索 |
| 负责人角色 | 平台管理员、销售主管 |
| 更新时间 | 2026-07-17 |

## 2. 页面概述

**业务背景**：经纪人需要在同一页面理解其负责线索关联客户的联系信息、需求、关联楼盘/房源、跟进时间线和下一动作。**页面目标**：以 `lead_id` 为唯一行和操作对象展示需求，支持持续跟进和规范的转化/流失处理。**成功标准**：负责人可在一个详情中完成该 `lead_id` 的需求维护、跟进、带看记录、推荐和阶段更新，销售主管可检查团队进度。

**MVP 范围**：客户档案、当前线索需求、偏好区域/预算、关联楼盘房源、时间线、带看记录、转派、流失和转化。**非目标**：合同、支付、结算、完整预约带看排期、自动推荐。

## 3. 用户与权限

| 角色 | 数据范围与操作权限 |
|---|---|
| 平台管理员 | 全部城市、团队、客户和线索；可查看 README 的 `MVP-R1` 发布常量、审计全部操作，并可在填写原因后执行跟进更正覆盖；本期无 UI 配置入口。 |
| 运营人员 | 授权城市的客户与线索只读脱敏数据；不可分配、认领、审批、维护销售过程或变更 MVP-R1 发布常量。 |
| 销售主管 | 所属团队及授权城市；可查看团队客户，执行团队内分配、团队认领、转派、回收、阶段回退和跟进更正审批。团队认领须按公海客户规则选择团队内经纪人作为受益归属人；不可变更 MVP-R1 发布常量。 |
| 经纪人 | 仅本人负责客户及其当前线索；可维护需求、跟进、推荐、带看和阶段证据。 |
| 客服 | 授权城市内本人录入或获授权咨询客户的只读脱敏数据；可补充接入资料，不可维护销售过程、分配、认领、审批或变更 MVP-R1 发布常量。 |

客户手机号默认脱敏；经纪人本人、所属团队销售主管和平台管理员可按字段权限查看职责范围内的完整手机号。导出须另有导出权限且继承当前数据范围。

客户档案与线索分离：客户是由 `customer_id` 标识的持久联系人/企业资料；线索是由 `lead_id` 标识的一次咨询或需求，关联客户、来源、归因和生命周期。客户本身不使用销售阶段，本页的阶段字段始终指“当前线索阶段”。本页一行严格对应一个有权限访问的 `lead_id`，不按 `customer_id` 聚合；同一客户的多需求或多负责人必须分行，详情、跟进、编辑、推荐、阶段、转派和导出操作均精确传递该 `lead_id`。

## 4. 页面入口与关联

- 菜单入口：客户线索 > 我的客户；线索被分配或从公海认领后进入。
- 上游：线索池分配、转派和公海认领；客户档案可包含多个历史线索。
- 下游：跟进记录详情、关联楼盘/房源详情、工作台下次跟进待办、公海客户、已转化客户的只读历史。
- 返回列表时保留 URL 中筛选、排序和分页；直接访问客户详情时必须同时校验客户和当前线索的数据权限。

## 5. 页面结构

页面包含固定筛选区、线索列表和线索详情抽屉/详情页。列表一行一个 `lead_id`，默认按下次跟进时间升序、逾期优先；无下次跟进时间的记录按最后跟进时间升序。详情按“客户档案、当前需求、偏好位置与预算、关联楼盘/房源、跟进时间线、带看记录、阶段与归属历史”排序，当前需求和下一动作固定展示。

## 6. 查询与筛选

| 控件 | 规则 |
|---|---|
| 关键词 | 客户姓名、公司、手机号和客户/线索 ID；手机号遵守字段权限。 |
| 当前线索阶段 | 使用第 10 节统一枚举，多选。 |
| 意向城市、区域、商圈 | 城市级联启用的区域/商圈；支持偏好地点多选。 |
| 面积、预算、入驻时间、租期 | 数值区间和日期；面积最小值不得大于最大值，预算保留币种、周期和单位。 |
| 下次跟进、最后跟进、带看时间 | 北京时间范围；支持“今日到期”“已逾期”。 |
| 负责人、团队、来源、是否有关联房源 | 均受数据权限限制；来源包含前台详情咨询、列表快速咨询、委托找房、电话咨询等。 |

重置后回到默认排序；查询状态保留在 URL 和当前会话中。

## 7. 列表或内容展示

| 字段 | 展示规则 |
|---|---|
| 线索 / 客户 | `lead_id`、姓名、公司、按权限脱敏手机号；一行只对应一个 `lead_id`，同一客户的多需求或多负责人分行，客户 ID 不随合并变化。 |
| 当前需求摘要 | 意向城市/区域、面积、预算、入驻时间；无值显示 `-`。 |
| 当前线索阶段 | 仅使用第 10 节阶段；不得显示客户阶段。 |
| 偏好地点 | 区域、商圈、地铁/办公点等由需求字段提供的偏好。 |
| 负责人/团队 | 当前处理线索负责人；历史负责人不覆盖。 |
| 关联楼盘/房源 | 数量及最近关联对象；已下架对象显示历史标识。 |
| 最后跟进/下次动作 | 时间、跟进结果摘要、下次时间和逾期标记。 |
| 操作 | 详情、跟进、编辑需求、推荐、阶段更新、申请转派；所有请求精确携带该行 `lead_id`，按权限显示。 |

列表支持按下次跟进、最后跟进、创建时间排序，空值固定排末；默认每页 20 条，可切换 20/50/100 条。

## 8. 详情及表单

| 分组 | 字段、规则和保存行为 |
|---|---|
| 客户档案 | 姓名、规范化手机号、公司名称；手机号变更需重新查重。客户档案去重由销售主管或平台管理员在授权流程中执行，来源客户 ID 保留映射和历史。 |
| 当前需求 | 意向城市必填；偏好区域/商圈、面积、预算、工位数、入驻日期、租期、特殊要求；区间校验与线索池一致。保存更新当前 `lead_id`，不改写其他历史线索。 |
| 关联对象 | 可关联楼盘和当前满足 README 统一有效供给谓词的房源；候选查询与保存提交均须重新校验完整谓词，不能只校验已上架。待复核、举报暂停、媒体不完整、位置/商户关系或资格失效的房源不得新增推荐；历史推荐后续失效仍保留提交时快照和 ID，但不继续作为有效候选。 |
| 带看记录 | 记录实际带看时间、楼盘/房源、参与人、结果和备注；不是预约排期。带看记录可作为进入“带看”的业务证据。 |
| 下一动作 | 跟进方式、内容、下次跟进时间；内容至少 5 个字。下次时间保存后生成站内待办。 |
| 转化/流失 | 已转化记录时间和说明；已流失必须选择字典中的流失原因。二者均写日志且不可用作合同或结算凭据。 |

保存时使用版本号校验；发生冲突时阻止覆盖，展示最新字段和更新时间。附件遵循单个 10MB 上限、权限和审计规则。

## 9. 核心操作与流程

1. **新增跟进**：负责人选择电话、微信、到访、带看或其他，填写内容、结果、关联对象和下次时间；成功后更新当前线索的最后跟进时间、结果摘要和工作台提醒。
2. **推荐与需求更新**：推荐时至少关联一条已上架房源；需求变化保存到当前线索并记录前后值。系统不自动改变阶段，负责人根据证据选择阶段。
3. **阶段变更**：按第 10 节前进或由销售主管在有原因时回退；进入带看需存在实际带看记录，进入谈判需有谈判说明，进入已转化或已流失需填写对应信息。
4. **转派/释放**：负责人提交目标经纪人或公海释放原因；销售主管或平台管理员在归属事务内锁定/串行化受益经纪人，并权威查询 `COUNT(owner_id=该经纪人 AND 非终态 AND merge_status!=已合并)`，成功后的结果不得超过 100 条。转派属于重新分配：成功时必须在同一事务中持久化 `assignment_succeeded` 归属事件及本次 `ownership_acquired_at`，并为新负责人新建首条有效人工跟进 SLA 实例，持久化 `runtime_policy_version=MVP-R1` 及实际 SLA 秒数；不得沿用前一负责人或创建时间的时限。容量不使用可增减持久化计数，转交、终态、入公海和合并自然改变查询集合。释放不删除既有归属事件或 SLA 实例。转派和释放均保留全量归属历史并发送站内通知。
5. **线索合并**：规范化手机号命中时提交手工合并。默认目标按“非终态优先、原始 `created_at` 最早优先、相同时 `lead_id` 字典序最小优先”确定；多个非终态线索的需求、阶段或归属冲突时，由销售主管选择目标并填写原因；仅有终态候选时按原始 `created_at`、`lead_id` 顺序确定。客户档案与线索分别保留 ID 映射，目标线索聚合来源、归因、负责人和时间线。

## 10. 状态与业务规则

### 10.1 唯一线索生命周期

本模块四个页面使用完全相同的线索生命周期：`新建（new） → 待分配（pending assignment） → 跟进中（following） → 有效商机（valid opportunity） → 带看（viewing） → 谈判（negotiating） → 已转化（converted）/已流失（lost）`。

- 新建为接入校验通过的短暂状态，随后进入待分配；分配给经纪人后进入跟进中。
- 经需求确认可进入有效商机；有实际带看记录可进入带看；有谈判说明可进入谈判。允许销售主管补充原因后回退到跟进中或有效商机，历史不可覆盖。
- 已转化和已流失为终态，不能分配、认领或放入公海；已流失必须有流失原因。已转化仅表示线索转化事实，不表示合同、支付或结算完成。
- 公海是归属位置，不是阶段；新建、待分配、跟进中、有效商机、带看、谈判六个非终态线索可按规则进入或移出公海。

### 10.2 客户、线索与数据关系

客户是长期联系人/企业档案，不设销售阶段；线索是客户某次需求及其生命周期实例。一个客户可关联多个线索，列表优先展示当前处理线索；历史和终态线索只读保留。所有楼盘、房源、客户、线索和账号都使用不可变 ID，姓名、手机号和地址不是唯一主键。

合并来源线索写入非生命周期字段 `merge_status=已合并` 和 `merged_into_lead_id=目标 lead_id` 并转为只读；目标保留所有来源、来源参数、楼盘/房源归因、附件、归属、阶段、跟进和操作历史。被合并来源不进入新增、分配 SLA、首次有效跟进 SLA 或漏斗计数，所有指标只计目标一次。本文仅在新增线索、分配 SLA 和漏斗“新建”阶段使用目标及来源中最早原始 `created_at` 形成的 `effective_created_at`；其他指标按目标保留的最早有效业务事件计算。

每次分配成功或认领成功均在归属历史中持久化不可变事件 ID、事件类型、负责人和 `ownership_acquired_at`，并在同一事务创建 `first_valid_follow_up_sla_instance`，至少保存 `sla_instance_id`、`lead_id`、负责人、归属事件 ID、起算时间、`due_at=ownership_acquired_at+4` 小时、`runtime_policy_version=MVP-R1`、实际 SLA 秒数和关闭状态。预警和逾期均由该实例的 `due_at` 驱动。当前归属期的首条有效人工跟进关闭该实例；重新分配或重新认领必须新建实例，历史实例不可改写。更正为无效时，同一事务发布 `follow_up_invalidated` 并重算 `last_valid_follow_up_at`、`next_follow_up_at` 和阶段证据；若失效的是该归属周期首条有效跟进且实例仍属该周期，按原 `due_at` 重开/更新待办，已过期立即超时且不延长；回收从重算后的最后有效跟进或归属起点计时，阶段仅由该记录支撑时回退到最近有效证据阶段，终态只可经销售主管纠错流程处理。

### 10.3 MVP-R1 发布常量与事件快照

| 规则 | README 的 MVP-R1 发布常量 | 执行与事件快照 |
|---|---|---|
| 分配 SLA | 2 小时 | 待分配及成功分配/认领事件持久化 `runtime_policy_version=MVP-R1` 和实际秒数。 |
| 首条有效人工跟进 SLA | 最近一次取得归属的分配成功或认领成功时间 + 4 小时 | 每次取得归属时原子固化实例的起算、`due_at`、`runtime_policy_version=MVP-R1` 和实际秒数。 |
| 公海认领后归属保护期 | 24 小时 | 认领成功事件固化 `runtime_policy_version=MVP-R1`、实际秒数和保护截止时间。 |
| 无有效人工跟进回收 | 连续 72 小时 | 回收判定记录 `runtime_policy_version=MVP-R1` 和实际秒数；从重算后的最后有效跟进或归属起点计时。 |
| 每日成功认领上限 | 每名经纪人 20 条/北京时间自然日 | 以受益经纪人、北京时间日界的不可变 `claim_succeeded` 事件 `COUNT` 原子校验，只计成功事件。 |
| 活跃自有线索上限 | 每名经纪人 100 条 | 事务内锁定/串行化受益经纪人，权威 `COUNT(owner_id=该经纪人 AND 非终态 AND merge_status!=已合并)`；不持久化可增减计数。 |
| 跟进更正窗口 | 原记录创建后 24 小时 | 更正事件持久化 `runtime_policy_version=MVP-R1` 和实际窗口秒数；窗口内须销售主管审批，平台管理员可在填写原因后覆盖。 |

以上均为 README 定义的 `MVP-R1` 发布常量，本期不提供 UI 配置入口，也不得声称可按人、团队或运行时修改；相关事件必须持久化常量版本及实际值。销售主管可在所属团队执行分配、团队认领、回收与更正审批；团队认领按公海客户规则以受益经纪人为归属、配额和活跃线索校验对象。

## 11. 异常与边界

- 无当前负责人、客户仅有终态历史线索或负责人停用时，详情只读并提示销售主管转派或认领路径。
- 无权限、直达越权链接、字段越权和导出越权均拒绝访问，不能从关联对象绕过。
- 已下架/已出租房源不可新增推荐或带看关联；已保存的历史记录继续展示对象快照。
- 加载失败可重试；保存失败不丢失草稿；并发编辑提示刷新并对比最新变更。
- 客户手机号重复、城市停用、预算/面积区间非法、附件超限或跟进内容不足时定位字段并阻止提交。
- 重分配或重新认领并发、归属已变化或 SLA 实例创建失败时，归属事件和实例必须整体回滚；不得出现新负责人没有本次 `ownership_acquired_at`、沿用旧实例或以 `created_at` 补算的状态。

## 12. 数据与指标

实时数据来自客户、线索、归属历史、首次有效跟进 SLA 实例、跟进、楼盘、房源与操作日志；工作台待办按保存后实时更新，分析报表按日刷新。统计先将 `merged_into_lead_id` 映射到合并目标，排除 `merge_status=已合并` 的来源并只计目标一次。数据层保存归属、认领、SLA、回收和更正事件命中的 `runtime_policy_version=MVP-R1` 与实际值；README 的 `MVP-R1` 发布常量为分配 2 小时、首条有效人工跟进 4 小时、认领后保护 24 小时、无有效人工跟进回收 72 小时、每日成功认领 20 条、活跃自有线索 100 条和跟进更正 24 小时，本期无 UI 配置入口。

| 指标 | 口径 |
|---|---|
| 我的待跟进线索数 | 本人负责、当前线索非终态且下次跟进时间在统计时点前的去重 `lead_id` 数；不按客户聚合。 |
| 首次有效跟进及时率 | 分子为统计期内 `due_at` 落入周期、归属给本人且在该归属周期 `due_at` 前完成首条有效人工跟进的 `sla_instance_id` 数；分母为同口径已到期或已完成的全部 `sla_instance_id` 数。每次分配、认领、重分配和重认领均独立计数；未到期且未完成单列，起算和截止仅取该实例归属事件，不得使用 `created_at`、首次分配时间或按 `lead_id` 去重，明细保留关联 `lead_id`。 |
| 推荐房源率 | 统计期内至少有一次已上架房源推荐的非终态目标 `lead_id` 数 / 统计期内本人跟进的非终态目标 `lead_id` 数。 |
| 带看线索数 | 统计期内首次进入带看的目标 `lead_id` 数，来源线索不重复计数。 |
| 转化数 | 统计期内首次进入已转化的目标 `lead_id` 数，按目标保留的首次转化事件计算。 |

导出以当前列表为准，继承筛选、排序、数据和字段权限，时间到秒并记录导出审计。

## 13. 埋点、通知与审计

埋点：`customer_detail_viewed`、`lead_requirement_updated`、`follow_up_created`、`follow_up_invalidated`、`listing_recommended`、`viewing_record_created`、`lead_stage_changed`、`lead_transfer_requested`。新建进入待分配时通知销售主管，2 小时未分配时发送逾期通知；每次分配、转派或认领成功均向当前负责人展示该 SLA 实例的 `due_at`，到达 `due_at` 未关闭时预警并通知负责人和销售主管；跟进失效导致首条待办重开且已过期时立即发送超时提醒。下次跟进到期或逾期时通知负责人和销售主管；连续 72 小时无有效人工跟进触发回收时通知原负责人和销售主管；公海认领成功通知中展示 24 小时保护截止时间；转派完成通知新旧负责人。每日认领 20 条和活跃自有线索 100 条上限在提交时明确提示。24 小时内的跟进更正申请通知销售主管，审批结果通知原作者；平台管理员覆盖须通知原作者和所属销售主管并展示原因。审计记录操作者、客户/线索 ID、前后值、归属事件 ID、SLA 实例 ID、起算/截止时间、`runtime_policy_version=MVP-R1`、实际常量值、时间、IP、来源页面与结果；跟进作者和创建时间不可改写。

## 14. 验收标准

- **正常**：经纪人可维护本人客户的当前需求、推荐已上架房源、提交带看和跟进记录，并生成下次待办；默认首次分配 SLA 为 2 小时；每次分配、转派或认领成功均以本次取得归属时间生成首条有效人工跟进 SLA 实例，截止及未关闭预警均为 +4 小时。
- **权限**：经纪人不能查看他人客户，销售主管仅看所属团队且不能变更 MVP-R1 发布常量；运营人员和客服不能维护销售过程；未授权用户不能查看完整手机号或导出敏感列。
- **异常**：下架房源不能新增推荐；并发保存和手机号重复均阻止覆盖并保留用户输入。
- **数据**：列表一行一个 `lead_id`，同一客户多需求或多负责人分行，所有动作精确传递 `lead_id`；客户档案不出现销售阶段，公海不作为阶段；首次有效跟进及时率按 SLA 实例而非 `lead_id` 计量，分母为 `due_at` 落在统计期且已到期或已完成的全部实例；每日认领以不可变成功认领事件 `COUNT` 校验，活跃自有线索以事务内权威查询校验且不维护计数；所有规则按 README 的 `MVP-R1` 发布常量及事件持久化版本/实际值执行，本期无 UI 配置入口；合并来源写入 `merge_status=已合并` 与 `merged_into_lead_id` 并从新增、SLA、漏斗排除，目标只计一次且保留来源、归因和历史。

## 15. 依赖与风险

依赖线索池、跟进记录、楼盘/房源上下架状态、经纪人服务城市与团队、线索来源/流失原因字典、README 的 MVP-R1 发布常量、工作台提醒、操作日志和导出服务。风险：多线索客户的当前处理线索必须按合并目标与非终态归属确定；不得把客户档案状态误作线索生命周期。

## 16. 截图参考

截图能力编号与证据路径以 [README 第 6 节](../README.md#6-截图能力追溯矩阵) 为准。

| 参考截图 | 采用能力 | 未采用能力 |
|---|---|---|
| 参考截图1 | 委托找房需求中的区域、面积、租金偏好。 | 前台推荐广告。 |
| 参考截图2 | 顾问、楼盘和房源咨询归因。 | VR 与在线预约排期。 |
| 参考截图3 | 快速咨询与位置、面积、价格筛选条件。 | 品牌视觉样式。 |
| 参考截图4 | 房源、顾问与实际带看信息关联。 | 出售交易流程。 |

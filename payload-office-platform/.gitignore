.env
.env.local
.env*.local
.next
node_modules
dist
payload-types.ts
# CI 发布时注入代码包（见 .github/workflows/deploy.yml），不进仓库
build-info.json
*.tsbuildinfo
*.db
*.db-journal
*.sqlite
*.sqlite-journal
*.sqlite-*
# 本地上传目录只在本包根下，必须锚定 `/`：裸 `media` 会命中任意层级的同名
# 目录（如 src/domain/media/），让该目录下的新文件被 git add 静默拒收
/media/
/media-source/
public/images/
test-results
playwright-report

# Agent 过程产物（计划/设计/验证证据）不入库；仍在生效的规则请写进 .agent/ 或 docs/
docs/superpowers/
tests/evidence/

# 本地临时产物与抓取的原始数据（不入库）
tmp/
.tmp/
scripts/import-huizuxuanzhi/data/
.playwright
.baseline

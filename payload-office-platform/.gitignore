.env
.env.local
.env*.local
.next
node_modules
dist
payload-types.ts
# CI 发布时注入代码包（见 .github/workflows/deploy.yml），不进仓库
build-info.json
*.tsbuildinfo
*.db
*.db-journal
*.sqlite
*.sqlite-journal
*.sqlite-*
# 本地磁盘存储的上传目录。**必须带前导斜杠锚定到项目根**：裸 `media` 会匹配任意层级
# 名为 media 的目录，`src/domain/media/` 曾因此被静默忽略——文件在本地好好的，
# 提交里却没有，CI 直接 module not found（OPT-070 与 OPT-069 各自独立踩到过）。
/media/
# OPT-069：水印烘焙前的干净原件备份目录，本地磁盘存储模式下落在这里。
/media-source/
public/images/
test-results
playwright-report

# Agent 过程产物（计划/设计/验证证据）不入库；仍在生效的规则请写进 .agent/ 或 docs/
docs/superpowers/
tests/evidence/

# 本地临时产物与抓取的原始数据（不入库）
tmp/
.tmp/
scripts/import-huizuxuanzhi/data/
.playwright
.baseline

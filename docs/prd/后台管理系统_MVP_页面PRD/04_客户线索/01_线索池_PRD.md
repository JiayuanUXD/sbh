# 01 线索池 PRD

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 编号 | 04-01 |
| 版本 | V1.1 |
| 状态 | 评审修订 |
| 所属模块 | 客户线索 |
| 负责人角色 | 平台管理员、销售主管 |
| 更新时间 | 2026-07-17 |

## 2. 页面概述

**业务背景**：前台详情页咨询、列表页快速咨询、委托找房和电话咨询需要被统一承接、去重和分配，避免漏跟或重复联系。

**页面目标**：提供可追溯的线索接入、校验、分配与跟进入口。**成功标准**：合格线索均可定位来源、客户、归属、时限及全量历史；超过首次响应 SLA 的线索可被识别和处理。

**MVP 范围**：接入、导入、去重、合并、单条和批量分配、转派、拒绝接入、放入公海及查看历史。**非目标**：自动派单、电话外呼或录音、IM、完整预约带看、合同或结算流程。

## 3. 用户与权限

| 角色 | 数据范围与权限 |
|---|---|
| 平台管理员 | 全部城市、团队和线索；可查看 README 的 `MVP-R1` 发布常量、分配、合并、导出，并可在填写原因后执行更正覆盖；本期无 UI 配置入口。 |
| 运营人员 | 授权城市；可导入、补全和查看脱敏线索，不可分配、认领、审批或变更 MVP-R1 发布常量。 |
| 销售主管 | 所属团队及授权城市；可在团队内分配、发起团队认领、转派、回收、审批跟进更正和查看团队完整手机号。团队认领须按公海客户规则选择团队内经纪人作为受益归属人；不可变更 MVP-R1 发布常量。 |
| 经纪人 | 仅本人负责线索；可查看本人完整手机号、跟进、申请转派或释放。 |
| 客服 | 授权城市内本人录入或获授权咨询线索；可录入、补全和查看脱敏信息，不可分配、认领、审批或变更 MVP-R1 发布常量。 |

客户手机号默认显示为 `138****1234`；完整号码和导出敏感列分别受字段权限与导出权限控制。任何查询、导出和操作均按城市、团队和本人数据权限过滤。

## 4. 页面入口与关联

- 菜单入口：客户线索 > 线索池；可从工作台待分配、超时线索进入并保留筛选条件。
- 上游：前台楼盘/房源详情咨询（保留 `building_id`、`listing_id`）、列表快速咨询、委托找房、电话咨询、客服录入、运营人员 xlsx 导入。
- 下游：分配后进入我的客户；放入公海后进入公海客户；新增记录及历史跳转跟进记录；已转化线索可下钻对应客户。
- 前置条件：意向城市为启用城市；候选经纪人在职且服务该城市。返回列表时保留 URL 查询条件、分页和排序。

## 5. 页面结构

从上到下依次为：标题及新增/导入/批量操作区、展开的筛选区、结果与排序栏、分页列表、右侧详情抽屉。默认按创建时间倒序，页码默认 20 条，支持 20/50/100 条；批量工具栏仅在勾选后固定显示。

详情抽屉按“客户档案、当前线索需求、来源与楼盘/房源归因、负责人与 SLA、跟进及流转历史”分组。窄屏仅作为最低限度响应适配，后台以桌面操作为主。

## 6. 查询与筛选

| 控件 | 默认值与规则 |
|---|---|
| 关键词 | 客户姓名、公司、脱敏或授权后的完整手机号、线索 ID；空值返回权限范围内结果。 |
| 来源/来源参数 | 多选来源；来源参数显示页面、活动、渠道和 `building_id`/`listing_id`，无归因显示“未关联”。 |
| 当前线索阶段 | 多选；使用第 10 节的唯一枚举。 |
| 负责人/是否待分配 | 负责人受数据权限限制；待分配对应 `pending assignment`。 |
| 意向城市、区域、商圈 | 城市联动区域和商圈；只能选择启用基础数据。 |
| 面积、预算、入驻日期 | 闭区间；最小值不得大于最大值；预算同时按数值、币种、周期和单位查询。 |
| 创建、最后跟进、下次跟进时间 | 北京时间，列表到分钟；导出与日志到秒。 |
| SLA/公海状态 | 首次跟进超时、下次跟进超时、是否在公海。 |

重置恢复页面默认值；有效筛选同步写入 URL，刷新和返回均保留。

## 7. 列表或内容展示

| 字段 | 规则 |
|---|---|
| 线索 ID / 创建时间 | 不可变 ID；创建时间为北京时间。 |
| 客户 | 显示姓名、公司及按权限脱敏手机号；点击打开详情。 |
| 当前需求 | 意向城市/区域、面积、预算、入驻日期摘要；缺失显示 `-`。 |
| 当前线索阶段 | 仅显示第 10 节枚举及相应阶段时间。 |
| 来源与归因 | 来源、页面/渠道参数和关联楼盘/房源名称；名称跳转时按权限校验。 |
| 负责人 / 所属团队 | 未分配显示“待分配”；已离职历史负责人保留名称和停用标识。 |
| 最后跟进 / 下次动作 | 时间、结果摘要和超时标记。 |
| 操作 | 详情、分配、转派、跟进、放入公海、导出；按角色和状态显示。 |

允许按创建时间、最后跟进时间、下次跟进时间排序；排序字段为空时排在末尾。列表不展示物理删除数据。

## 8. 详情及表单

新增、编辑与导入映射使用同一字段校验：

| 分组 | 字段与校验 |
|---|---|
| 客户档案 | 姓名、规范化手机号必填；公司名称可空。客户是以规范化手机号为主要识别键的长期联系人档案，**不承载线索阶段**。 |
| 线索需求 | 意向城市、来源必填；区域/商圈可多选；面积、预算最小值不得大于最大值；工位数为正整数；入驻日期、租期、特殊要求可选。 |
| 来源归因 | 来源枚举：详情咨询、列表快速咨询、委托找房、电话咨询、客服录入、运营人员导入、其他；保留来源参数、`building_id`、`listing_id`，关联对象不存在时保留 ID 与历史快照。 |
| 负责人 | 经纪人新建的线索可预填本人；客服或运营人员录入后进入待分配；候选人必须在职、服务意向城市。归属事务内锁定/串行化受益经纪人后，权威查询 `COUNT(owner_id=该经纪人 AND 非终态 AND merge_status!=已合并)`；分配、转派或认领成功后的结果不得超过 100 条。 |
| 附件 | 可附咨询截图或需求文件；单个不超过 10MB，记录文件名、类型、存储键和上传人；禁止将手机号写入文件名。 |

导入仅支持 xlsx，必填列为客户姓名、手机号、意向城市、线索来源。提交前预校验，错误行提供可下载报告；版本号或更新时间不一致时阻止覆盖并提示刷新。

## 9. 核心操作与流程

1. **接入和去重**：保存前规范化手机号（去空格、区号和分隔符后统一格式），按手机号查询全部历史 Customer；重复 Lead 判定仅查询最近 30 天，即 `created_at >= 接入时点-2592000秒`。接入判定持久化 `runtime_policy_version=MVP-R1`、`lead_dedup_window_seconds=2592000`、查询时点和命中 ID。命中时展示客户、负责人、当前线索阶段和最近跟进时间。用户可取消、在既有客户下新建独立需求，或提交手工合并。默认目标按“非终态优先、原始 `created_at` 最早优先、相同时 `lead_id` 字典序最小优先”确定；若多个非终态线索的需求、阶段或归属冲突，禁止自动合并，由销售主管选择目标并填写原因；仅有终态候选时按原始 `created_at`、`lead_id` 顺序确定目标。平台管理员可在权限范围内执行并审计合并。
2. **拒绝接入**：缺必填字段、非法号码、重复导入且选择不创建独立需求时拒绝创建线索；记录拒绝原因和原始导入行。拒绝结果不是客户或线索生命周期状态。
3. **分配/批量分配**：销售主管或平台管理员选择一名符合城市、在职和承接能力条件的经纪人；批量操作先展示影响数量、逐条校验和二次确认，返回成功、失败及原因。归属事务内锁定/串行化受益经纪人，以 `COUNT(owner_id=该经纪人 AND 非终态 AND merge_status!=已合并)` 作权威容量查询；成功后不得超过 100 条，禁止使用可递增/回滚的持久化计数。首次分配 SLA 从有效创建时间起 2 小时；每次分配成功均在同一事务中写入不可变 `assignment_succeeded` 归属事件及其成功时间 `ownership_acquired_at`，并为该次归属生成首条有效人工跟进 SLA 实例，持久化 `runtime_policy_version=MVP-R1` 及实际 SLA 秒数。销售主管仅在所属团队执行；分配后写系统记录并通知经纪人。
4. **转派与释放**：当前负责人申请转派；销售主管或平台管理员确认后变更负责人并保留前负责人。转派属于重新分配，必须以本次 `assignment_succeeded` 的 `ownership_acquired_at` 新建 SLA 实例，不继承前一负责人的实例或时限。符合第 10 节条件的非终态线索可放入公海，需填写原因；公海认领成功后进入 24 小时归属保护期，并以本次认领成功时间新建 SLA 实例。
5. **SLA、纠错与回收**：有效人工跟进须由有权限用户提交，内容和结果校验通过，且当前有效版本未被撤回。首条有效人工跟进 SLA 的起算为当前归属最近一次取得归属的 `ownership_acquired_at`（分配成功或认领成功时间），`due_at=ownership_acquired_at+4` 小时；预警、逾期和及时性均只读取该归属周期的未关闭 SLA 实例，绝不使用 `created_at` 或 `effective_created_at`。当前归属期内首条有效人工跟进成功后关闭该实例；重新分配或重新认领时另建实例。跟进更正为无效时，必须在同一事务将版本置无效、发布不可变 `follow_up_invalidated`，并重算 `last_valid_follow_up_at`、`next_follow_up_at` 与阶段证据；若失效记录是该归属周期首条有效跟进且实例仍属同一归属周期，则按原 `due_at` 重开或更新首次跟进待办，已过期立即标记超时且不得延长。回收计时从重算后的最后有效跟进开始，无有效跟进时从归属起点开始；若阶段仅由该记录支撑，回退至最近有效证据阶段，终态不得自动回退，须走销售主管纠错流程。认领后 24 小时保护期内不因 72 小时规则回收。待分配、首次有效跟进预警/超时和回收均生成站内提醒，不发送短信或微信。

所有危险操作均二次确认；状态、负责人、合并、导入、导出与公海操作写入操作日志。

## 10. 状态与业务规则

### 10.1 唯一线索生命周期

本模块四个页面使用完全相同的线索生命周期：`新建（new） → 待分配（pending assignment） → 跟进中（following） → 有效商机（valid opportunity） → 带看（viewing） → 谈判（negotiating） → 已转化（converted）/已流失（lost）`。

- 新建仅表示接入校验通过但尚未完成路由；系统随即进入待分配，分配给经纪人后进入跟进中。
- 跟进中可因需求确认进入有效商机；实际到访或看房记录可进入带看；谈判进入谈判。阶段可前进，不得跳过缺少的业务证据；销售主管可在详情中补充原因后回退到跟进中或有效商机，系统保留流转历史。
- 已转化、已流失为终态，不能分配、认领或放入公海；已流失必须选择流失原因。已转化仅记录转化事实和时间，不扩展合同、支付或结算。
- 公海是归属位置，不是阶段。新建、待分配、跟进中、有效商机、带看、谈判六个非终态线索可在符合公海规则时转入或转出。

### 10.2 对象与一致性

客户是持久的联系人/企业档案，使用 `customer_id` 标识；线索是某次咨询或需求，使用 `lead_id` 标识并关联一个客户。客户不使用上述阶段，页面中展示的“当前线索阶段”是该客户当前处理线索的阶段。楼盘和房源通过不可变 `building_id`、`listing_id` 关联；已下架或删除的展示对象不可新增推荐，但历史归因和历史关联必须保留。

线索合并不改变生命周期枚举。合并目标保留全部来源、来源参数、楼盘/房源归因、附件、负责人及阶段/跟进/操作历史；每个来源线索写入非生命周期字段 `merge_status=已合并` 和 `merged_into_lead_id=目标 lead_id`，并转为只读。被合并来源不进入新增、分配 SLA、首次有效跟进 SLA 或漏斗计数；所有指标只计目标一次。本文仅在“新增线索数”“分配 SLA 起算”和漏斗“新建”阶段明确使用目标及来源中最早的原始 `created_at` 作为 `effective_created_at`，其他指标使用合并目标保留的对应最早有效业务事件。

每次分配成功或认领成功必须在归属历史中持久化不可变事件 ID、事件类型、负责人和 `ownership_acquired_at`；同一事务创建 `first_valid_follow_up_sla_instance`，至少保存 `sla_instance_id`、`lead_id`、负责人、归属事件 ID、起算时间、`due_at`、`runtime_policy_version=MVP-R1`、实际 SLA 秒数和关闭状态。预警和逾期均由该实例的 `due_at` 驱动。重分配、重新认领均生成新实例，历史实例保留且不被改写。跟进被更正为无效时，同一事务发布 `follow_up_invalidated` 并重算有效跟进、下一待办和阶段证据；首条有效跟进失效且实例仍属同一归属周期时，按原 `due_at` 重开待办，逾期立即超时，不得延长。

### 10.3 MVP-R1 发布常量与事件快照

| 规则 | README 的 MVP-R1 发布常量 | 执行与事件快照 |
|---|---|---|
| 分配 SLA | 2 小时 | 待分配及成功分配/认领事件持久化 `runtime_policy_version=MVP-R1` 和实际秒数；销售主管在所属团队执行分配。 |
| 首条有效人工跟进 SLA | 最近一次取得归属的分配成功或认领成功时间 + 4 小时 | 每次取得归属时原子固化实例的起算、`due_at`、`runtime_policy_version=MVP-R1` 和实际秒数；预警/逾期均按该实例截止时间触发。 |
| 公海认领后归属保护期 | 24 小时 | 认领成功事件固化 `runtime_policy_version=MVP-R1`、实际秒数和保护截止时间。 |
| 无有效人工跟进回收 | 连续 72 小时 | 回收判定记录 `runtime_policy_version=MVP-R1` 和实际秒数；从重算后的最后有效跟进或归属起点计时。 |
| 每日成功认领上限 | 每名经纪人 20 条/北京时间自然日 | 以受益经纪人、北京时间日界的不可变 `claim_succeeded` 事件 `COUNT` 原子校验，只计成功事件，不维护可变配额计数。 |
| 活跃自有线索上限 | 每名经纪人 100 条 | 事务内锁定/串行化受益经纪人，权威 `COUNT(owner_id=该经纪人 AND 非终态 AND merge_status!=已合并)`；不持久化可增减计数。 |
| 跟进更正窗口 | 原记录创建后 24 小时 | 更正事件持久化 `runtime_policy_version=MVP-R1` 和实际窗口秒数；窗口内须销售主管审批，平台管理员可在填写原因后覆盖。 |

以上均为 README 定义的 `MVP-R1` 发布常量，本期不提供 UI 配置入口，也不得声称可按人、团队或运行时修改；相关事件必须持久化常量版本及实际值。销售主管可在所属团队执行分配、团队认领、回收与更正审批；团队认领按公海客户规则以受益经纪人为归属、配额和活跃线索校验对象。

## 11. 异常与边界

- 加载中显示骨架；无结果显示空状态；查询/保存失败显示错误原因和重试入口，不清空已输入数据。
- 无菜单、操作或字段权限时隐藏入口并在直达 URL 时返回无权限页；不得通过搜索或导出绕过数据权限。
- 并发分配、合并或编辑以版本号校验，旧版本提交失败并展示最新负责人、阶段和更新时间。
- 导入存在部分错误时不写入错误行，成功行可确认后写入；重复检测失败或来源对象失效时明确列出行号和原因。
- 经纪人停用或城市不匹配时不可作为新负责人；其已有历史保留，销售主管必须转派未终态线索。

## 12. 数据与指标

数据源为客户、线索、楼盘、房源、经纪人、归属历史、首次有效跟进 SLA 实例、跟进记录、操作日志和导出任务。列表实时查询；经营与分析指标按日刷新。指标先将 `merged_into_lead_id` 映射到合并目标，排除 `merge_status=已合并` 的来源线索并只计目标一次。数据层保存归属、认领、SLA、回收和更正事件命中的 `runtime_policy_version=MVP-R1` 与实际值；README 的 `MVP-R1` 发布常量为分配 2 小时、首条有效人工跟进 4 小时、认领后保护 24 小时、无有效人工跟进回收 72 小时、每日成功认领 20 条、活跃自有线索 100 条和跟进更正 24 小时，本期无 UI 配置入口。

| 指标 | 口径 |
|---|---|
| 新增线索数 | 统计期内 `effective_created_at` 落入周期、接入未被拒绝的合并目标 `lead_id` 数；`effective_created_at` 仅在本指标取目标及来源最早原始 `created_at`。 |
| 待分配线索数 | 当前阶段为待分配且无负责人的非终态 `lead_id` 数。 |
| 分配及时率 | 从 `effective_created_at` 起 2 小时内首次成功分配的目标 `lead_id` 数 / 统计期内应分配目标 `lead_id` 数。 |
| 首次有效跟进及时率 | 分子为统计期内 `due_at` 落入周期、在该归属周期 `due_at` 前完成首条有效人工跟进的 `sla_instance_id` 数；分母为同口径已到期或已完成的全部 `sla_instance_id` 数。每次分配、认领、重分配和重认领均独立计数；未到期且未完成单列，不得使用 `created_at`、`effective_created_at`、首次分配时间或按 `lead_id` 去重，明细保留关联 `lead_id`。 |
| 有效商机数 | 统计期内首次进入有效商机的去重 `lead_id` 数。 |

金额按数值、币种、计价周期和单位保存，面积以平方米原值保存、展示最多一位小数。导出继承当前筛选、排序和数据权限，手机号按字段权限脱敏，记录操作者、条件、字段、数量和时间。

## 13. 埋点、通知与审计

埋点：`lead_created`、`lead_dedup_hit`、`lead_merged`、`lead_assigned`、`lead_transferred`、`lead_released_to_public_pool`、`follow_up_invalidated`、`lead_exported`。通知：新建进入待分配时通知所属销售主管，2 小时未分配时发送逾期通知；每次分配或认领成功均向当前负责人展示该 SLA 实例的 `due_at`，到达 `due_at` 未关闭时预警并通知负责人和销售主管；跟进失效导致首条待办重开且已过期时立即发送超时提醒；连续 72 小时无有效人工跟进触发回收时通知原负责人和销售主管；认领后的 24 小时保护截止时间随成功通知一并展示。每日认领 20 条和活跃自有线索 100 条上限在提交时返回明确结果；24 小时内的跟进更正申请通知销售主管，平台管理员超窗覆盖时通知原作者和所属销售主管并展示原因。审计至少记录操作者、对象类型/不可变 ID、操作类型、前后值、归属事件 ID、SLA 实例 ID、起算/截止时间、来源页面、`runtime_policy_version=MVP-R1`、实际常量值、时间（秒）、IP 和结果。

## 14. 验收标准

- **正常**：来自四类前台/电话来源的合格咨询均创建客户关联线索，并保留楼盘或房源归因；销售主管可在所属团队单条和批量分配，默认分配 SLA 为 2 小时；每次分配成功或认领成功均以最近一次取得归属时间生成首条有效人工跟进 SLA 实例，截止及未关闭预警均为 +4 小时。
- **权限**：经纪人仅能查看和跟进本人线索；无完整手机号或导出权限的用户只能获得脱敏数据。
- **异常**：重复手机号命中去重窗口时必须提供新需求、合并或取消；旧版本分配不得覆盖新负责人；达到每日认领 20 条或活跃自有线索 100 条时拒绝对应认领或归属操作。
- **数据**：生命周期仅出现第 10 节枚举；公海仅表示归属；首次有效跟进及时率按 SLA 实例而非 `lead_id` 计量，分母为 `due_at` 落在统计期且已到期或已完成的全部实例；24 小时认领保护、72 小时无有效人工跟进回收和 24 小时更正窗口均持久化 `runtime_policy_version=MVP-R1` 与实际值；合并来源写入 `merge_status=已合并` 与 `merged_into_lead_id`，从新增、SLA、漏斗排除，目标只计一次并保留所有原始来源、归因和历史。

## 15. 依赖与风险

依赖账号/角色、经纪人服务城市、城市区域、线索来源和原因字典、楼盘房源主数据、文件存储、站内通知、README 的 MVP-R1 发布常量、导入导出和操作日志。风险：手机号规范化或前台来源参数缺失会降低去重与归因质量；须以合并映射、`runtime_policy_version=MVP-R1`、实际值和原始快照支持重算与审计。

## 16. 截图参考

截图能力编号与证据路径以 [README 第 6 节](../README.md#6-截图能力追溯矩阵) 为准。

| 参考截图 | 采用能力 | 未采用能力 |
|---|---|---|
| 参考截图1 | 委托找房入口、区域/面积/租金需求参数。 | 广告投放与视觉样式。 |
| 参考截图2 | 楼盘顾问咨询、预约看房意向与楼盘归因。 | 前台展示与 VR。 |
| 参考截图3 | 快速咨询、位置和筛选参数归因。 | 热门楼盘广告位。 |
| 参考截图4 | 顾问咨询、房源/楼盘关联。 | 出售交易流程。 |

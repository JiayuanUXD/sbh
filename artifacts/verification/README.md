# 验证证据目录

每个任务使用独立目录：

```text
artifacts/verification/<task-id>/
├── README.md
├── screenshots/
└── logs/
```

任务证据 `README.md` 至少记录：

- 构建 SHA/工作树状态；
- 环境和数据前提；
- 执行命令及退出码；
- 浏览器路由、操作、预期和实际；
- 截图/日志相对路径；
- 未执行项和剩余风险。

不得保存密钥、`.env`、数据库、完整手机号、姓名、留言、原始 IP 或其他个人信息。大体积、临时或敏感日志应保留在受控系统中，仅在此记录安全链接。

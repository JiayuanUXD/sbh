# MP-104 验证记录

## 已执行（Node 侧）

- 小程序完整测试：Node 22.23.2，26 个测试文件、421 个用例通过。
- 小程序双 TypeScript 检查：通过；`project:check`：通过。
- 已补充“首页 → 找房 → 首条真实房源 slug → 详情页 marker”的冒烟合同与 fake automator 测试。
- Web：typecheck 通过；300 个测试文件中 295 个通过、5 个按既有条件跳过，4098 个用例中 4073 个通过、25 个跳过。
- Web 生产构建退出码为 0；构建期按既有配置守卫记录了本机未配置 COS，并使用既有城市静态参数降级，不代表完成生产配置验收。
- Web ESLint：0 个错误、23 个既有警告；本任务未把既有 `<img>` 与 Hook 警告伪装为新增回归。
- `git diff --check`：通过。

## 微信开发者工具实测

- 2026-08-27 使用本机 Stable `1.06.2409140` 尝试两次 `devtools:smoke`，均以退出码 1 结束；没有预览、上传或部署。
- 首次确认工具服务端口关闭；CLI 开启流程等待端口文件超时。随后在工具安全设置中只读复核，服务端口已显示开启（端口 `31431`）。该设置当前保持开启。
- 工程已在模拟器编译并打开首页，但 develop 环境请求 `http://127.0.0.1:3717` 被微信“request 合法域名”校验拒绝，首页进入错误态，无法到达 `#home-ready`，因此列表与详情真实冒烟没有继续。
- 本轮未代替用户关闭合法域名校验；该安全相关设置需用户在操作当下确认。即使后续允许本机域名，还需同时启动本地 API 后再复跑。

## 未执行 / 待验证

- 预发布隔离 fixture、咨询写入、重复提交及数据库单 Lead 证据：未执行，待隔离环境。
- 微信开发者工具首页/列表/详情闭环：已尝试但受本机合法域名校验阻断；iOS/Android 真机、弱网、键盘/安全区、授权拒绝与隐私弹窗：未执行，待设备与账号。
- VoiceOver/TalkBack 关闭弹层后的触发点焦点恢复：未执行。微信公开 `button` 属性没有程序化 focus 能力，代码已移除无效的 focus token，不以静态标记冒充真机结果。
- request/downloadFile 合法域名、AppID/AppSecret、签名密钥和隐私政策配置：未执行，待受控环境。
- `pnpm ci:preview`、上传、部署和生产数据库写入：未执行；本任务明确不执行。

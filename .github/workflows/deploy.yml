# Git 自动部署 -> CloudBase CloudRun
#
# 触发：push 到 master 且改动 payload-office-platform/ 或本 workflow 文件；
#       也可在 Actions 页面手动触发 (workflow_dispatch)。
#
# 两段式（OPT-016）：
#   quality  - 在 CI runner 上跑 lint/typecheck/test/build/迁移预检，作为发布质量门。
#              失败则 deploy 不跑。产物不传递（deploy 重新 checkout 干净源码）。
#   deploy   - 锁定 @cloudbase/cli@3.6.4；部署新版本（灰度 0% 流量）-> 切 10% 灰度 ->
#              冒烟（/api/health + /）-> 通过则 traffic promote 全量，失败则 traffic rollback。
#
# 机制：用 CloudBase CLI (tcb) 上传 payload-office-platform/ 的 ZIP 到云端，
#       平台在线 docker build 并发布新版本。与 MCP manageCloudRun(deploy) 同一底层。
#       CI 端不装项目依赖、不构建--构建在平台发生（quality job 的 build 仅作质量门，
#       产物留在 quality runner，不进 ZIP）。
#
# 迁移单次执行：容器启动时跑 scripts/migrate-locked.ts（PG advisory lock 互斥 + 幂等），
#       多实例并发拉起只有一个实例真正跑迁移。CI 不单独跑迁移。
#
# 鉴权：腾讯云 CAM 子账号 API 密钥 (SecretId/SecretKey)，tcb login --apiKeyId/--apiKey。
#       子账号需绑定 CAM 策略：
#         - QcloudAccessForTCBRole
#         - QcloudAccessForTCBRoleInAccessCloudBaseRun
#       密钥注入到 GitHub Secrets：TCB_SECRET_ID / TCB_SECRET_KEY / TCB_ENV_ID
#
# 环境变量：tcb cloudrun deploy 没有 env-vars 参数--服务级环境变量
#       (DATABASE_URL / PAYLOAD_SECRET / NODE_ENV) 已在控制台/MCP 配好，
#       代码部署只更新版本，不清空服务配置。无需每次重传。
#       首次跑 CI 后建议到控制台核对环境变量仍在。

name: Deploy to CloudBase CloudRun

on:
  push:
    branches: [master]
    paths:
      - 'payload-office-platform/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

# 部署互相排队，不中途取消（避免并发发布互相覆盖版本）
concurrency:
  group: deploy-sbh
  cancel-in-progress: false

jobs:
  # ---------- 质量门：发布前必须通过 ----------
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: payload-office-platform
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8.6.1

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: 迁移预检（目录与索引一致性 + 危险项扫描）
        # 绕过 npm script 的 --env-file=.env.local（CI 无该文件）；
        # migrations 子命令纯扫描文件，不读 env。
        run: pnpm exec tsx scripts/preflight.ts migrations

      - name: Build（质量门：验证能构建通过，产物不上传）
        run: pnpm build

  # ---------- 部署：灰度 -> 冒烟 -> 全量/回滚 ----------
  deploy:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      # 非交互模式：所有确认提示（telemetry 询问、--force 确认）自动按默认值走。
      # 否则 CI 无 tty，提示库收到 EOF 会以 exit 130 中断。
      CLOUDBASE_CI: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install CloudBase CLI（锁版本，避免 @latest 漂移破坏命令语义）
        run: npm install -g @cloudbase/cli@3.6.4

      - name: tcb login (CAM API key)
        run: tcb login --apiKeyId "${{ secrets.TCB_SECRET_ID }}" --apiKey "${{ secrets.TCB_SECRET_KEY }}"

      - name: Deploy 新版本（灰度，初始 0% 流量）
        # --force 跳过 Y/n 确认；CLOUDBASE_CI=1 跳过 telemetry；
        # 但"是否启用灰度部署？"是 list 类型提示，--force 不挡，CI 无 tty 会 exit 130。
        # 用 printf 喂回车 -> 选默认项。
        # tcb cloudrun deploy 默认把新版本发布为灰度版本（流量 0），线上流量仍在旧版本。
        # 失败保护：新版本创建/构建/启动失败时不会进入灰度流程，旧版本继续承接。
        run: |
          printf '\n\n\n' | tcb -e "${{ secrets.TCB_ENV_ID }}" cloudrun deploy \
            -s sbh \
            --source "${{ github.workspace }}/payload-office-platform" \
            --port 80 \
            --force

      - name: 等待新版本就绪 + 切 10% 灰度
        # sleep 等容器启动 + migrate-locked 跑完；再切 10% 流量到新版本。
        # stable + canary 必须等于 100。
        run: |
          sleep 30
          tcb -e "${{ secrets.TCB_ENV_ID }}" cloudrun traffic \
            -s sbh \
            --stable 90 \
            --canary 10

      - name: Smoke test（灰度版本健康检查）
        id: smoke
        run: |
          # 灰度 10% 期间多次请求，提高命中新版本概率；
          # 同时验证整体服务可用（旧版本不被新版本部署影响）。
          URL="https://sbh-286300-10-1253925058.sh.run.tcloudbase.com"
          fail=0
          for i in $(seq 1 10); do
            code=$(curl -s -o /dev/null -w "%{http_code}" "$URL/api/health")
            echo "[$i] GET /api/health -> HTTP $code"
            if [ "$code" != "200" ]; then fail=1; fi
          done
          # /api/health 响应体必须含 status:ok
          body=$(curl -s "$URL/api/health")
          echo "$body"
          echo "$body" | grep -q '"status":"ok"' || fail=1
          # 首页可达（SSR 不崩）
          home=$(curl -s -o /dev/null -w "%{http_code}" "$URL/")
          echo "GET / -> HTTP $home"
          if [ "$home" != "200" ]; then fail=1; fi
          if [ "$fail" = "1" ]; then
            echo "::error::冒烟失败，将触发回滚"
            exit 1
          fi
          echo "✅ 冒烟通过"

      - name: Promote 全量发布
        if: success()
        run: |
          tcb -e "${{ secrets.TCB_ENV_ID }}" cloudrun traffic promote -s sbh
          echo "✅ 全量发布完成"

      - name: Rollback（冒烟或 promote 失败时切回稳定版本）
        if: failure()
        run: |
          echo "::error::发布失败，回滚流量到稳定版本"
          tcb -e "${{ secrets.TCB_ENV_ID }}" cloudrun traffic rollback -s sbh || true
          exit 1
